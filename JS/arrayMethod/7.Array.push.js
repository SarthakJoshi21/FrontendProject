// push method helps to add an element at the last of an array and it also changes the length of the array
const arr=['r','i','y'];
console.log(arr.length);
console.log(arr.push('a'));
console.log(arr);
console.log(arr.length);